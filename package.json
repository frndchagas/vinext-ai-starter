{
  "name": "vinext-ai-starter",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.4.0",
  "engines": {
    "bun": "1.4.0",
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/api",
    "apps/web",
    "contracts/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "bun install --frozen-lockfile && docker compose up -d --wait postgres redis mailpit caddy && composer --working-dir apps/api run setup",
    "dev": "docker compose up -d --wait postgres redis mailpit caddy && API_PORT=${API_PORT:-18000} APP_URL=${APP_URL:-http://localhost:13000} BROADCAST_CONNECTION=reverb CACHE_STORE=redis DB_CONNECTION=pgsql DB_DATABASE=starter DB_HOST=127.0.0.1 DB_PASSWORD=starter DB_PORT=${POSTGRES_PORT:-15432} DB_USERNAME=starter MAIL_HOST=127.0.0.1 MAIL_MAILER=smtp MAIL_PORT=${MAILPIT_SMTP_PORT:-11025} NEXT_PUBLIC_REVERB_APP_KEY=${REVERB_APP_KEY:-starter-key} QUEUE_CONNECTION=redis REDIS_HOST=127.0.0.1 REDIS_PORT=${REDIS_PORT:-16379} REVERB_APP_ID=${REVERB_APP_ID:-starter} REVERB_APP_KEY=${REVERB_APP_KEY:-starter-key} REVERB_APP_SECRET=${REVERB_APP_SECRET:-starter-secret} REVERB_HOST=127.0.0.1 REVERB_PORT=${REVERB_PORT:-19080} REVERB_SCHEME=http REVERB_SERVER_PORT=${REVERB_PORT:-19080} SESSION_DRIVER=redis WEB_PORT=${WEB_PORT:-13100} turbo run dev --filter=web --filter=api",
    "infra:down": "docker compose down",
    "infra:up": "docker compose up -d --wait postgres redis mailpit caddy",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build",
    "config:check": "composer --working-dir apps/api validate --strict && docker compose config --quiet",
    "check": "bun run config:check && turbo run format:check lint typecheck test build",
    "graph": "turbo run build --graph=stdout"
  },
  "devDependencies": {
    "turbo": "2.10.11"
  }
}
